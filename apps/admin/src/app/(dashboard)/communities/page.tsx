import { PagePlaceholder } from '@/components/page-placeholder';

export default function Page() {
  return (
    <PagePlaceholder title="Communities" description="Moderate communities, memberships and posts." step={16} />
  );
}
