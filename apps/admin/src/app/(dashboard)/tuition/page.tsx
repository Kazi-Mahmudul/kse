import { PagePlaceholder } from '@/components/page-placeholder';

export default function Page() {
  return (
    <PagePlaceholder title="Tuition & Tutors" description="Verify tutor profiles, subjects and tuition requests." step={15} />
  );
}
