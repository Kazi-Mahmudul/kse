import { PagePlaceholder } from '@/components/page-placeholder';

export default function Page() {
  return (
    <PagePlaceholder title="Platform Settings" description="Support contact, feature flags, maintenance mode and app version." step={20} />
  );
}
