import { PagePlaceholder } from '@/components/page-placeholder';

export default function Page() {
  return (
    <PagePlaceholder title="Opportunities" description="Create, review and publish internships, scholarships, events, workshops and mentorship listings." step={7} />
  );
}
