import { PagePlaceholder } from '@/components/page-placeholder';

export default function Page() {
  return (
    <PagePlaceholder title="Master Data" description="Manage universities, departments, subjects, skills and tags." step={7} />
  );
}
